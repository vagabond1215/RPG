# RPG Refactor Base

- The repository root is now the refactor base.
- The previous root project content was moved to `legacy_root/`.
- `assets` remains available at `/assets`.
- Run `npm i` and then `npm run typecheck`.
